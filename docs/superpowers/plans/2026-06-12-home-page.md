# Home Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build `/home/index.html` — the new home page (hero, two-tier "Latest News" with an auto-cycling featured carousel, and an "Explore the Research" gallery mosaic) — plus its supporting JSON data and render scripts, a `/news/index.html` full news feed, and retarget the root splash page's redirect from `day-version/` to `/home/`.

**Architecture:** Two new data files (`data/news.json`, `data/gallery.json`) hold all migrated content. `assets/js/render-news.js` reads `news.json` and renders (a) the home page's featured carousel + "More News" list, and (b) the `/news/` full feed — both from the same data file, so adding a news item never requires touching HTML/CSS. `assets/js/render-gallery.js` reads `gallery.json` and renders the 5-tile mosaic. Both scripts follow `assets/js/partials.js`'s pattern from Plan 1: an IIFE, `fetch` + `.then` chain, `try`/`catch`-equivalent `.catch` that logs and leaves the section empty. New pages use Plan 1's "Page Template" verbatim and append new component CSS to `assets/css/style.css`.

**Tech Stack:** Static HTML/CSS/vanilla ES5 JS, no build step, served from the repo root (GitHub Pages user-site, so `/assets/...`, `/data/...` resolve correctly both locally via `npx serve` and in production).

---

## Plan Series Note (Plan 2 of 6)

This plan builds on **Plan 1** (`docs/superpowers/plans/2026-06-12-foundation-theming-and-navigation.md`), which must be implemented first. It uses, without modification:
- CSS tokens/typography/layout/buttons/nav/footer/modal already defined in `assets/css/style.css`.
- The "Page Template" `<head>`/`<body>` structure (copied verbatim into each new page below).
- `assets/js/theme.js` and `assets/js/partials.js` (loaded by every page, unchanged).

**Scope note — `/news/index.html`:** the design spec's §3 "Page Designs" doesn't have a dedicated subsection for `/news/`, but `data/nav.json` (Plan 1) already links to `/news/`, and the spec says "Items with neither `featured` value don't appear on the home page but still appear on `/news/`." Since `/news/` shares `data/news.json` and `render-news.js` with the home page's news section, building it here (Task 6) is the natural fit — no separate plan needed.

**Cross-plan contracts** (decisions made here that later plans must honor):

1. **Project slug registry** — two `news.json` entries below contain "Read More" links to `/research/project.html?id=<slug>`. **Plan 5** (Research) must give `projects.json` entries these exact slugs:
   - `perovskite-nanolasers` — corresponds to the legacy `research/research-project/prj4_pvsklaser.html` ("Miniaturized Layered Perovskite Lasers").
   - `templated-perovskite-optoelectronics` — corresponds to the legacy `research/research-project/prj2_templatemhp.html` ("Templated Crystallization in Perovskite Optoelectronics").
2. **Forward links that 404 until later plans run** (expected during phased rollout, not a bug):
   - `gallery.json`'s 4 small tiles link to `/research/` — valid once **Plan 5** creates `research/index.html`.
   - `news.json`'s "New Postdoc" large card links to `/about/` — valid once **Plan 3** creates `about/index.html`.
   - The two "Read More" links above — valid once **Plan 5** creates `research/project.html`.

---

## File Structure

```
data/
  news.json                 (new — 11 entries, migrated from BODY/news-body.html)
  gallery.json              (new — 5 entries, migrated from day-version/index.html gallery)
assets/
  css/style.css             (modified — append "Shared Section Header", "Home: Latest News",
                             "News Feed", "Home: Gallery Mosaic", and "Hero" sections)
  js/
    render-news.js          (new — renderFeaturedNews() + renderNewsList())
    render-gallery.js        (new — renderGalleryMosaic())
  img/
    news/                    (new — 11 images copied from news_img/)
    gallery/                 (new — 5 images copied from TOC/)
home/
  index.html                 (new — hero + Latest News + Explore the Research)
news/
  index.html                  (new — full news feed)
index.html                    (modified — retarget splash redirect day-version/ -> /home/)
```

---

## Task 1: News data, images, and styles

**Files:**
- Create: `data/news.json`
- Create: `assets/img/news/2602_1.jpg` ... (11 images, copied from `news_img/`)
- Modify: `assets/css/style.css`

- [ ] **Step 1: Create the news data file**

Write `data/news.json`:

```json
[
  {
    "date": "Feb 2026",
    "title": "New Postdoc",
    "tag": "People",
    "image": "/assets/img/news/2602_1.jpg",
    "link": "/about/",
    "excerpt": "We warmly welcome postdoctoral researcher Dr. Le Wei, whose work on photonic devices and micro/nanofabrication will lead our new efforts in miniaturized chiroptics and integrated biosensing.",
    "body": "<p>We warmly welcome postdoctoral researcher Dr. Le Wei. Dr. Wei received his Ph.D. in Electrical Engineering from Iowa State University under the supervision of <a href=\"https://www.engineering.iastate.edu/people/profile/menglu/\" target=\"_blank\"><u>Prof. Meng Lu</u></a> and <a href=\"http://www.memslab.net/\" target=\"_blank\"><u>Prof. Liang Dong</u></a>. His research focuses on photonic devices and micro/nanofabrication, with experience in semiconductor processing and optical instrumentation. Dr. Wei will lead our efforts in miniaturized chiroptics and integrated biosensing.</p>",
    "featured": "large"
  },
  {
    "date": "Jan 2026",
    "title": "Layered Perovskite Room-Temperature Nanolasers",
    "tag": "Publication",
    "image": "/assets/img/news/2601_1.png",
    "link": "https://pubs.acs.org/doi/full/10.1021/jacs.5c14431",
    "excerpt": "Meticulous crystallization control of phase-pure quasi-2D tin-iodide perovskite nanowires enables color-tunable, room-temperature lasing, opening the door to a new platform for nanoscale photonics.",
    "body": "<p>Practical room-temperature (RT) operation of nanowire lasers requires efficient gain materials. Our recent <a href=\"https://pubs.acs.org/doi/full/10.1021/jacs.5c14431\" target=\"_blank\"><u>JACS</u></a>, provided an answer through meticulous crystallization control of phase-pure quasi-2D tin-iodide perovskite nanowires. These nanowires support color-tunable NIR, RT optically pumped lasing, with thresholds as low as 75.8 μJ/cm², cavity quality factors exceeding 3000, and negligible degradation over more than one million pulses. This work opens the door to using layered perovskite nanowires as a promising platform for nanoscale electronics and miniaturized photonic devices. Thanks to all the collaborators!</p><p>Read More: <a href=\"/research/project.html?id=perovskite-nanolasers\" target=\"_blank\"><u>Miniaturized Layered Perovskite Lasers</u></a>.</p>",
    "featured": "large"
  },
  {
    "date": "Dec 2025",
    "title": "Microscope Installation",
    "image": "/assets/img/news/2512_2.png",
    "body": "<p>We’re welcoming Russ and Charles from Hunt Optics &amp; Imaging for the installation of our brand-new Olympus IX85 microscope.</p>",
    "featured": "small"
  },
  {
    "date": "Dec 2025",
    "title": "Ionic Liquid x Perovskite Solar Cell",
    "tag": "Publication",
    "image": "/assets/img/news/2512_1.png",
    "link": "https://www.nature.com/articles/s41560-025-01906-6",
    "excerpt": "A new ionic liquid additive regulates perovskite growth and stabilizes buried interfaces, pushing power conversion efficiency to 25.9% with markedly improved long-term stability.",
    "body": "<p>Led by Dr. Wenzhan Xu, our paper using ionic liquids to enhance the stability of perovskite solar cells has been published in <a href=\"https://www.nature.com/articles/s41560-025-01906-6\" target=\"_blank\"><u>Nature Energy</u></a>. A new ionic liquid MEM-MIM-Cl was used to regulate perovskite growth and stabilize buried interfaces. Solar cells incorporating MEM-MIM-Cl achieved a power conversion efficiency of 25.9% and retained 90% initial performance after 1,500 hrs' 1-sun illumination and 90°C thermal stress - surpassing prior benchmarks under milder ageing conditions. Many thanks to all the collaborators!</p><p>Read More: <a href=\"/research/project.html?id=templated-perovskite-optoelectronics\" target=\"_blank\"><u>Templated Crystallization in Perovskite Optoelectronics</u></a>.</p>",
    "featured": "large"
  },
  {
    "date": "Nov 2025",
    "title": "The first cohort",
    "image": "/assets/img/news/2511_2.png",
    "body": "<p>We warmly welcome Wai Loon Chooi and Yue Wang as the first Ph.D. students in the lab! Wish you all the best in your future scientific discovery and innovation.</p>",
    "featured": "small"
  },
  {
    "date": "Nov 2025",
    "title": "AIChE 2025",
    "image": "/assets/img/news/2511_1.jpg",
    "body": "<p>Another great year with Area 8E - Electronic and Photonic Materials at the 2025 AIChE Conference - fantastic to reconnect and meet new faces!</p>",
    "featured": "small"
  },
  {
    "date": "Oct 7 2025",
    "title": "First Molecule",
    "image": "/assets/img/news/2510_1.jpg",
    "body": "<p>Rotation student Ichchha has successfully synthesized our first molecule in the lab. Image showing Ichchha in front of her first reaction. Congratulation!</p>"
  },
  {
    "date": "Sep 24 2025",
    "title": "Prof. Jinsang Kim's Professorship Ceremony",
    "image": "/assets/img/news/2509_3.jpg",
    "body": "<p>Wenhao's Ph.D. advisor, Prof. Jinsang Kim, has been named the Raoul Kopelman Collegiate Professor at the University of Michigan. Today marks his official professorship ceremony. Here are the <a href=\"https://macro.engin.umich.edu/jinsang-kim-named-raoul-kopelman-collegiate-professor-of-science-engineering/\" target=\"_blank\"><u>news</u></a>, <a href=\"https://leccap.engin.umich.edu/leccap/player/r/KWIPJv\" target=\"_blank\"><u>recording</u></a> of the ceremony, and <a href=\"https://www.flickr.com/photos/138562590@N05/albums/72177720329268804/\" target=\"_blank\"><u>photo gallery</u></a>. CONGRATS to the best mentor and the most complete person!</p><p>(p.c. Michigan MSE - Kim group @ceremony)</p>"
  },
  {
    "date": "Sep 2025",
    "title": "Miniaturized Layered Perovskite Lasers",
    "image": "/assets/img/news/2509_1.jpg",
    "body": "<p>Following the submission of two new papers on lead-free room temperature layered perovskite nanolasers, we invite you to read our executive summary on this breakthrough technology <a href=\"/research/project.html?id=perovskite-nanolasers\" target=\"_blank\"><u>here</u></a>.</p>"
  },
  {
    "date": "July 2025",
    "title": "Optics Lab Kicked Off",
    "image": "/assets/img/news/2507_1.JPEG",
    "body": "<p>We inherited a laser table thanks to the generous support of the Department of Chemistry.</p>"
  },
  {
    "date": "June 2025",
    "title": "W. Shao Laboratory was founded",
    "image": "/assets/img/news/news_default.png",
    "body": "<p>Wenhao moved to UGA Chemistry. The group will explore purely organic and organic–inorganic hybrid materials, with an initial emphasis on their photonic, electronic, and quantum applications.</p>"
  }
]
```

This migrates all 11 entries from `BODY/news-body.html`, newest first. Three get `"featured": "large"` (auto-cycling carousel, with `tag`/`link`/`excerpt`): New Postdoc, the JACS nanolasers paper, and the Nature Energy paper — these were the most substantive of the previously `data-featured="true"` items. Three get `"featured": "small"` (static "More News" list): Microscope Installation, The first cohort, AIChE 2025. The remaining 5 have no `featured` key — they'll appear on `/news/` only. The two internal "Read More" links now point to `/research/project.html?id=<slug>` per the slug registry above (previously `/which-version/research/research-project/prj4_pvsklaser.html` and `prj2_templatemhp.html`).

- [ ] **Step 2: Copy news images**

Run (from repo root, in PowerShell):

```powershell
New-Item -ItemType Directory -Force assets/img/news | Out-Null
Copy-Item news_img/2602_1.jpg assets/img/news/2602_1.jpg
Copy-Item news_img/2601_1.png assets/img/news/2601_1.png
Copy-Item news_img/2512_2.png assets/img/news/2512_2.png
Copy-Item news_img/2512_1.png assets/img/news/2512_1.png
Copy-Item news_img/2511_2.png assets/img/news/2511_2.png
Copy-Item news_img/2511_1.jpg assets/img/news/2511_1.jpg
Copy-Item news_img/2510_1.jpg assets/img/news/2510_1.jpg
Copy-Item news_img/2509_3.jpg assets/img/news/2509_3.jpg
Copy-Item news_img/2509_1.jpg assets/img/news/2509_1.jpg
Copy-Item news_img/2507_1.JPEG assets/img/news/2507_1.JPEG
Copy-Item news_img/news_default.png assets/img/news/news_default.png
```

Verify:

```powershell
(Get-ChildItem assets/img/news).Count
```

Expected: `11`

- [ ] **Step 3: Validate the JSON**

```powershell
$news = Get-Content data/news.json -Raw | ConvertFrom-Json
$news.Count
($news | Where-Object { $_.featured -eq 'large' }).Count
($news | Where-Object { $_.featured -eq 'small' }).Count
```

Expected output:

```
11
3
3
```

- [ ] **Step 4: Append news + news-feed component styles**

Append to the end of `assets/css/style.css`:

```css

/* ==========================================================================
   Shared Section Header
   ========================================================================== */
.section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-2);
  margin-bottom: var(--space-3);
}

/* ==========================================================================
   Home: Latest News
   ========================================================================== */
.home-news__grid {
  display: grid;
  grid-template-columns: 2fr 1fr;
  gap: var(--space-4);
  align-items: start;
}

.news-featured {
  position: relative;
  min-height: 420px;
  border-radius: 12px;
  overflow: hidden;
  background: var(--color-surface);
  border: 1px solid var(--color-border);
}

.news-card {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  opacity: 0;
  visibility: hidden;
  transition: opacity 0.6s ease;
}

.news-card.active {
  opacity: 1;
  visibility: visible;
  z-index: 1;
}

.news-card__media {
  position: relative;
  height: 220px;
}

.news-card__media img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.news-card__dots {
  position: absolute;
  bottom: 0.75rem;
  left: var(--space-2);
  display: flex;
  gap: 0.4rem;
}

.news-card__dot {
  width: 8px;
  height: 8px;
  padding: 0;
  border: none;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.5);
  cursor: pointer;
}

.news-card__dot.active {
  background: #ffffff;
}

.news-card__body {
  flex: 1;
  padding: var(--space-3);
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
}

.news-card__tag {
  color: var(--color-accent);
}

.news-card__title {
  font-size: 1.25rem;
}

.news-card__excerpt {
  margin: 0;
  flex: 1;
}

.news-more h3 {
  margin-bottom: var(--space-2);
}

.news-list {
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
}

.news-list a {
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
  padding: var(--space-2);
  border: 1px solid var(--color-border);
  border-radius: 8px;
  transition: border-color 0.15s ease, background 0.15s ease;
}

.news-list a:hover {
  border-color: var(--color-accent);
  background: var(--color-surface);
}

.news-list__date {
  font-size: 0.8125rem;
}

.news-list__title {
  font-weight: 700;
}

@media (max-width: 760px) {
  .home-news__grid {
    grid-template-columns: 1fr;
  }

  .news-featured {
    min-height: 380px;
  }
}

/* ==========================================================================
   News Feed (/news/)
   ========================================================================== */
.news-feed {
  display: flex;
  flex-direction: column;
  gap: var(--space-4);
}

.news-feed__item {
  display: grid;
  grid-template-columns: 280px 1fr;
  gap: var(--space-3);
  padding-bottom: var(--space-4);
  border-bottom: 1px solid var(--color-border);
}

.news-feed__item:last-child {
  border-bottom: none;
  padding-bottom: 0;
}

.news-feed__media img {
  width: 100%;
  height: 180px;
  object-fit: cover;
  border-radius: 8px;
}

.news-feed__date {
  display: block;
  margin-bottom: 0.25rem;
}

.news-feed__content p {
  margin-top: var(--space-1);
}

.news-feed__content p:first-child {
  margin-top: 0;
}

@media (max-width: 700px) {
  .news-feed__item {
    grid-template-columns: 1fr;
  }

  .news-feed__media img {
    height: 220px;
  }
}
```

- [ ] **Step 5: Commit**

```bash
git add data/news.json assets/img/news/ assets/css/style.css
git commit -m "Add news data, news images, and news/news-feed styles"
```

---

## Task 2: News render script

**Files:**
- Create: `assets/js/render-news.js`

- [ ] **Step 1: Create the render script**

Write `assets/js/render-news.js`:

```javascript
(function () {
  function escapeAttr(str) {
    return String(str).replace(/"/g, '&quot;');
  }

  function renderCarousel(mount, items) {
    if (items.length === 0) return;

    var dotsHtml = items.map(function (_, i) {
      return '<button class="news-card__dot' + (i === 0 ? ' active' : '') + '" data-index="' + i + '" aria-label="Show story ' + (i + 1) + '"></button>';
    }).join('');

    mount.innerHTML = items.map(function (item, index) {
      var tag = item.tag ? '<span class="news-card__tag text-label">' + item.tag + '</span>' : '';
      return '' +
        '<div class="news-card' + (index === 0 ? ' active' : '') + '" data-index="' + index + '">' +
          '<div class="news-card__media">' +
            '<img src="' + item.image + '" alt="' + escapeAttr(item.title) + '">' +
            '<div class="news-card__dots">' + dotsHtml + '</div>' +
          '</div>' +
          '<div class="news-card__body">' +
            tag +
            '<h3 class="news-card__title">' + item.title + '</h3>' +
            '<p class="news-card__excerpt text-muted">' + (item.excerpt || '') + '</p>' +
            '<a class="link-arrow" href="' + item.link + '" target="_blank" rel="noopener">Read more &rarr;</a>' +
          '</div>' +
        '</div>';
    }).join('');

    if (items.length < 2) return;

    var current = 0;
    var cards = mount.querySelectorAll('.news-card');
    var timer;

    function updateDots(index) {
      var dots = mount.querySelectorAll('.news-card__dot');
      dots.forEach(function (dot) {
        dot.classList.toggle('active', Number(dot.getAttribute('data-index')) === index);
      });
    }

    function show(index) {
      cards[current].classList.remove('active');
      current = index;
      cards[current].classList.add('active');
      updateDots(current);
    }

    function next() {
      show((current + 1) % items.length);
    }

    function start() {
      timer = setInterval(next, 5000);
    }

    function stop() {
      clearInterval(timer);
    }

    mount.querySelectorAll('.news-card__dot').forEach(function (dot) {
      dot.addEventListener('click', function () {
        show(Number(dot.getAttribute('data-index')));
        stop();
        start();
      });
    });

    mount.addEventListener('mouseenter', stop);
    mount.addEventListener('mouseleave', start);
    mount.addEventListener('focusin', stop);
    mount.addEventListener('focusout', start);
    mount.addEventListener('touchstart', function () { stop(); start(); }, { passive: true });

    start();
  }

  function renderMoreList(mount, items) {
    mount.innerHTML = items.map(function (item) {
      return '' +
        '<li><a href="/news/">' +
          '<span class="news-list__date text-muted">' + item.date + '</span>' +
          '<span class="news-list__title">' + item.title + '</span>' +
        '</a></li>';
    }).join('');
  }

  function renderFeaturedNews() {
    var featuredMount = document.getElementById('news-featured');
    var moreMount = document.getElementById('news-more-list');
    if (!featuredMount && !moreMount) return;

    fetch('/data/news.json')
      .then(function (res) { return res.json(); })
      .then(function (items) {
        if (featuredMount) {
          renderCarousel(featuredMount, items.filter(function (n) { return n.featured === 'large'; }));
        }
        if (moreMount) {
          renderMoreList(moreMount, items.filter(function (n) { return n.featured === 'small'; }));
        }
      })
      .catch(function (err) {
        console.error('Failed to load news:', err);
      });
  }

  function renderNewsList() {
    var mount = document.getElementById('news-feed');
    if (!mount) return;

    fetch('/data/news.json')
      .then(function (res) { return res.json(); })
      .then(function (items) {
        mount.innerHTML = items.map(function (item) {
          return '' +
            '<li class="news-feed__item">' +
              '<div class="news-feed__media"><img src="' + item.image + '" alt="' + escapeAttr(item.title) + '"></div>' +
              '<div class="news-feed__body">' +
                '<span class="news-feed__date text-muted">' + item.date + '</span>' +
                '<h2>' + item.title + '</h2>' +
                '<div class="news-feed__content">' + item.body + '</div>' +
              '</div>' +
            '</li>';
        }).join('');
      })
      .catch(function (err) {
        console.error('Failed to load news:', err);
      });
  }

  renderFeaturedNews();
  renderNewsList();
})();
```

`renderFeaturedNews()` powers the home page (`#news-featured` carousel + `#news-more-list`); `renderNewsList()` powers `/news/` (`#news-feed`). Both run on every page — each checks for its mount element(s) and no-ops if absent, so the one script works on both pages.

- [ ] **Step 2: Syntax-check the script**

```powershell
node --check assets/js/render-news.js
```

Expected: no output (success).

- [ ] **Step 3: Commit**

```bash
git add assets/js/render-news.js
git commit -m "Add render-news.js for home carousel, more-news list, and news feed"
```

---

## Task 3: Gallery data, images, and styles

**Files:**
- Create: `data/gallery.json`
- Create: `assets/img/gallery/Map_purple-gold.jpg`, `twist_1.jpg`, `helical_1.jpg`, `bear.jpg`, `chocolate.jpg` (copied from `TOC/`)
- Modify: `assets/css/style.css`

- [ ] **Step 1: Create the gallery data file**

Write `data/gallery.json`:

```json
[
  {
    "image": "/assets/img/gallery/Map_purple-gold.jpg",
    "title": "Layered Perovskite Nanowires",
    "link": "https://www.science.org/doi/10.1126/science.adl0920"
  },
  {
    "image": "/assets/img/gallery/twist_1.jpg",
    "title": "Twisted Ribbons",
    "link": "/research/"
  },
  {
    "image": "/assets/img/gallery/helical_1.jpg",
    "title": "Helical Ribbons",
    "link": "/research/"
  },
  {
    "image": "/assets/img/gallery/bear.jpg",
    "title": "Halide Perovskite Crystals",
    "link": "/research/"
  },
  {
    "image": "/assets/img/gallery/chocolate.jpg",
    "title": "Chocolate Crystals",
    "link": "/research/"
  }
]
```

5 entries selected from the 7 tiles in `day-version/index.html`'s gallery section, for the "Asymmetric rows" mosaic layout (top row: 1 wide tile + 1 narrow tile; bottom row: 3 equal tiles): entry 1 is the wide top-left tile (the lab's flagship Science-published nanowire work, linking externally to its DOI — no dependency on later plans), entry 2 is the narrow top-right tile, and entries 3-5 fill the bottom row (artistic shots from the existing TOC gallery, linking to `/research/`). "Earth" (untitled/no caption) and "Miniaturized Layered Perovskite Lasers" (already covered by `news.json`'s items 2 and 9 above) were left out of this 5-tile set; both can be added to `/research/` directly in Plan 5.

- [ ] **Step 2: Copy gallery images**

Run (from repo root, in PowerShell):

```powershell
New-Item -ItemType Directory -Force assets/img/gallery | Out-Null
Copy-Item TOC/Map_purple-gold.jpg assets/img/gallery/Map_purple-gold.jpg
Copy-Item TOC/twist_1.jpg assets/img/gallery/twist_1.jpg
Copy-Item TOC/helical_1.jpg assets/img/gallery/helical_1.jpg
Copy-Item TOC/bear.jpg assets/img/gallery/bear.jpg
Copy-Item TOC/chocolate.jpg assets/img/gallery/chocolate.jpg
```

Verify:

```powershell
(Get-ChildItem assets/img/gallery).Count
```

Expected: `5`

- [ ] **Step 3: Validate the JSON**

```powershell
(Get-Content data/gallery.json -Raw | ConvertFrom-Json).Count
```

Expected: `5`

- [ ] **Step 4: Append gallery mosaic styles**

Append to the end of `assets/css/style.css`:

```css

/* ==========================================================================
   Home: Gallery Mosaic
   ========================================================================== */
.gallery-mosaic {
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
}

.gallery-mosaic__row {
  display: flex;
  gap: var(--space-2);
}

.gallery-mosaic__row--top {
  height: 320px;
}

.gallery-mosaic__row--bottom {
  height: 200px;
}

.gallery-mosaic__row--top .tile.t1 {
  flex: 1.6;
}

.gallery-mosaic__row--top .tile.t2 {
  flex: 1;
}

.gallery-mosaic__row--bottom .tile {
  flex: 1;
}

.tile {
  position: relative;
  display: block;
  border-radius: 12px;
  overflow: hidden;
  background-size: cover;
  background-position: center;
}

.tile__overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(to bottom, transparent 50%, rgba(20, 24, 33, 0.82) 100%);
  display: flex;
  align-items: flex-end;
  padding: var(--space-2);
}

.tile__title {
  color: #ffffff;
  font-weight: 700;
  font-size: 0.9375rem;
  line-height: 1.3;
}

.tile.t1 .tile__title {
  font-size: 1.25rem;
}

@media (max-width: 760px) {
  .gallery-mosaic__row {
    flex-direction: column;
    height: auto;
  }

  .gallery-mosaic__row .tile {
    flex: none;
    aspect-ratio: 4 / 3;
  }
}
```

- [ ] **Step 5: Commit**

```bash
git add data/gallery.json assets/img/gallery/ assets/css/style.css
git commit -m "Add gallery data, gallery images, and mosaic styles"
```

---

## Task 4: Gallery render script

**Files:**
- Create: `assets/js/render-gallery.js`

- [ ] **Step 1: Create the render script**

Write `assets/js/render-gallery.js`:

```javascript
(function () {
  function renderGalleryMosaic() {
    var mount = document.getElementById('gallery-mosaic');
    if (!mount) return;

    fetch('/data/gallery.json')
      .then(function (res) { return res.json(); })
      .then(function (items) {
        var classes = ['t1', 't2', 't3', 't4', 't5'];
        var tiles = items.map(function (item, index) {
          var external = /^https?:\/\//.test(item.link);
          var attrs = external ? ' target="_blank" rel="noopener"' : '';
          return '' +
            '<a class="tile ' + (classes[index] || '') + '" href="' + item.link + '" style="background-image:url(\'' + item.image + '\')"' + attrs + '>' +
              '<div class="tile__overlay"><span class="tile__title">' + item.title + '</span></div>' +
            '</a>';
        });

        mount.innerHTML =
          '<div class="gallery-mosaic__row gallery-mosaic__row--top">' + tiles.slice(0, 2).join('') + '</div>' +
          '<div class="gallery-mosaic__row gallery-mosaic__row--bottom">' + tiles.slice(2, 5).join('') + '</div>';
      })
      .catch(function (err) {
        console.error('Failed to load gallery:', err);
      });
  }

  renderGalleryMosaic();
})();
```

The first two entries in `gallery.json` form the top row (`t1` wide, `t2` narrow); the remaining three form the bottom row (`t3`-`t5`, equal width). External links (`http...`) open in a new tab; internal links (`/research/`) don't.

- [ ] **Step 2: Syntax-check the script**

```powershell
node --check assets/js/render-gallery.js
```

Expected: no output (success).

- [ ] **Step 3: Commit**

```bash
git add assets/js/render-gallery.js
git commit -m "Add render-gallery.js for home page mosaic"
```

---

## Task 5: Home page

**Files:**
- Create: `home/index.html`
- Modify: `assets/css/style.css`

- [ ] **Step 1: Append hero styles**

Append to the end of `assets/css/style.css`:

```css

/* ==========================================================================
   Hero (Home)
   ========================================================================== */
.hero {
  text-align: center;
  padding-top: var(--space-4);
}

.hero p {
  margin-left: auto;
  margin-right: auto;
}

.hero > * + * {
  margin-top: var(--space-1);
}

.hero__subtitle {
  color: var(--color-accent);
}

.hero__tagline {
  margin-top: var(--space-3) !important;
}
```

- [ ] **Step 2: Create the home page**

Write `home/index.html`:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-Y8E59MSLP3"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());

    gtag('config', 'G-0V2HS5NSS6');
  </script>

  <script src="/assets/js/theme.js"></script>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/assets/css/style.css">
  <link rel="icon" href="/assets/icons/SWH_logo.png">

  <meta name="description" content="W. Shao Laboratory — Electronic and Photonic Materials Discovery, Department of Chemistry, University of Georgia.">
  <meta name="robots" content="index,follow">
  <title>W. Shao Laboratory — Electronic and Photonic Materials Discovery</title>
</head>
<body>
  <div id="site-nav"></div>

  <main>
    <section class="container hero">
      <h1 class="text-display">W. Shao Laboratory</h1>
      <p class="text-lead hero__subtitle">Electronic and Photonic Materials Discovery</p>
      <p class="text-muted">Department of Chemistry &middot; University of Georgia</p>
      <p class="text-lead hero__tagline">Where chemistry meets light.</p>
    </section>

    <section class="container home-news">
      <div class="section-header">
        <h2>Latest News</h2>
        <a href="/news/" class="link-arrow">View all &rarr;</a>
      </div>
      <div class="home-news__grid">
        <div class="news-featured" id="news-featured"></div>
        <div class="news-more">
          <h3>More News</h3>
          <ul class="news-list" id="news-more-list"></ul>
        </div>
      </div>
    </section>

    <section class="container">
      <div class="section-header">
        <h2>Explore the Research</h2>
        <a href="/research/" class="link-arrow">View All Research &rarr;</a>
      </div>
      <div class="gallery-mosaic" id="gallery-mosaic"></div>
    </section>
  </main>

  <div id="site-footer"></div>

  <script src="/assets/js/render-news.js" defer></script>
  <script src="/assets/js/render-gallery.js" defer></script>
  <script src="/assets/js/partials.js" defer></script>
</body>
</html>
```

- [ ] **Step 3: Commit**

```bash
git add home/index.html assets/css/style.css
git commit -m "Add home page with hero, latest news, and gallery sections"
```

---

## Task 6: News feed page

**Files:**
- Create: `news/index.html`

- [ ] **Step 1: Create the news feed page**

Write `news/index.html`:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-Y8E59MSLP3"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());

    gtag('config', 'G-0V2HS5NSS6');
  </script>

  <script src="/assets/js/theme.js"></script>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/assets/css/style.css">
  <link rel="icon" href="/assets/icons/SWH_logo.png">

  <meta name="description" content="News — W. Shao Laboratory">
  <meta name="robots" content="index,follow">
  <title>News — W. Shao Laboratory</title>
</head>
<body>
  <div id="site-nav"></div>

  <main>
    <section class="container">
      <h1>News</h1>
      <ul class="news-feed" id="news-feed"></ul>
    </section>
  </main>

  <div id="site-footer"></div>

  <script src="/assets/js/render-news.js" defer></script>
  <script src="/assets/js/partials.js" defer></script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add news/index.html
git commit -m "Add news feed page"
```

---

## Task 7: Retarget splash redirect and verify end-to-end

**Files:**
- Modify: `index.html` (root splash page)

- [ ] **Step 1: Retarget the splash redirect**

`index.html` currently redirects to `day-version/` in four places. Make these four edits:

Edit 1 — iframe preload (around line 36):

```html
        preloadFrame.src = "day-version/";
```
becomes
```html
        preloadFrame.src = "/home/";
```

Edit 2 — prefetch link (around line 45):

```html
        link.href = "day-version/";
```
becomes
```html
        link.href = "/home/";
```

Edit 3 — main redirect (around line 55):

```html
            window.location.replace("day-version/");
```
becomes
```html
            window.location.replace("/home/");
```

Edit 4 — `<noscript>` fallback (around line 61):

```html
      <meta http-equiv="refresh" content="3; url=day-version/">
```
becomes
```html
      <meta http-equiv="refresh" content="3; url=/home/">
```

Nothing else in `index.html` changes — the GIF timing (2400ms/2000ms/400ms), black background, fade styles, `opening.gif` reference, and "W. Shao Laboratory ©2025" caption all stay as-is.

- [ ] **Step 2: Start a local static server**

Run (from repo root, leave running in its own terminal):

```powershell
npx --yes serve . -l 5000
```

Expected: output ending with something like `Accepting connections at http://localhost:5000`.

- [ ] **Step 3: Manual verification — splash redirect**

Open `http://localhost:5000/` and check:

- [ ] The black splash screen with `opening.gif` and "W. Shao Laboratory ©2025" appears.
- [ ] After ~2.4s the page fades out and you land on `http://localhost:5000/home/`.

- [ ] **Step 4: Manual verification — home page**

On `http://localhost:5000/home/`, check:

- [ ] **Hero**: "W. Shao Laboratory" renders large and bold (Display style); "Electronic and Photonic Materials Discovery" appears below it in the accent color; "Department of Chemistry · University of Georgia" appears in muted gray; "Where chemistry meets light." appears last, with a bit of extra space above it.
- [ ] **Latest News**: a large featured card on the left shows an image, a tag (e.g., "People"), a title, an excerpt, and a "Read more →" link. It crossfades to a different story roughly every 5 seconds. Three small dots appear over the bottom-left of the image; clicking a dot jumps to that story.
- [ ] Hover the featured card: the auto-rotation pauses (wait >5s and confirm the story doesn't change while hovering); move the mouse away and it resumes.
- [ ] To the right, "More News" lists 3 items (date + title): Microscope Installation, The first cohort, AIChE 2025. Clicking one navigates to `/news/`.
- [ ] **Explore the Research**: a mosaic of 5 image tiles appears in two rows — a top row with one wide tile ("Layered Perovskite Nanowires") and one narrower tile ("Twisted Ribbons"), and a bottom row with 3 equal tiles ("Helical Ribbons", "Halide Perovskite Crystals", "Chocolate Crystals") — each with a title overlaid on a gradient at the bottom of the image.
- [ ] Click the wide top-left mosaic tile — it opens the Science.org DOI page in a new tab. Click one of the other tiles — it navigates to `/research/` (expected 404 at this stage; `/research/` is built in Plan 5).
- [ ] Toggle dark mode (moon/sun icon in nav): hero, news cards, and gallery mosaic all remain readable — text stays legible against the dark background, and the dot indicators / gradient overlays remain visible against the photos.
- [ ] Resize to a narrow viewport (<760px): the news section stacks to one column (featured card on top, "More News" below), and the gallery mosaic stacks into a single column of 5 tiles, each at a 4:3 aspect ratio.
- [ ] In the browser console, confirm there are **no errors** on page load (clicking the two `/research/`-linked gallery tiles and the "New Postdoc" Read More link will 404 at this stage — expected until Plans 3 and 5 run — but page **load** itself is error-free).

- [ ] **Step 5: Manual verification — news feed page**

On `http://localhost:5000/news/`, check:

- [ ] All **11** news entries render, newest first (New Postdoc ... W. Shao Laboratory was founded), each with its image, date, title, and full body text (including inline links like the JACS/Nature Energy DOIs).
- [ ] The "New Postdoc" entry's links to Prof. Meng Lu and Prof. Liang Dong open in new tabs.
- [ ] The "Layered Perovskite Room-Temperature Nanolasers" entry's "Read More" link points to `/research/project.html?id=perovskite-nanolasers`.
- [ ] Toggle dark mode and resize to <700px: each entry's image stacks above its text, full width.

- [ ] **Step 6: Commit**

```bash
git add index.html
git commit -m "Retarget splash redirect from day-version/ to /home/"
```
